# DCID Backend SDK Go Server Environment Variables

# API Key (required)
export DCID_API_KEY=your-api-key-here

# Environment (optional, default: dev)
export DCID_ENVIRONMENT=dev

# Server Port (optional, default: 8080)
export PORT=8080
